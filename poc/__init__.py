"""Proof-of-Concept validations.

Each subdirectory is one PoC defined in ``docs/specs/nucleus_poc_plan.md``. PoCs are
exploratory and isolated — they may import any layer, may use any
library, and are NOT counted against the LOC budget (per
``[tool.nucleus] loc_exclude`` in ``pyproject.toml``).

PoCs graduate to ``src/nucleus/`` only after acceptance criteria pass.
"""
