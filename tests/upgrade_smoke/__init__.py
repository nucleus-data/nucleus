# tests/upgrade_smoke/ — dependency upgrade smoke tests (AGENTS.md §11.13).
# Each test file locks a component's behavioral contract so dlt minor bumps
# are caught before they silently change data semantics or APIs.
