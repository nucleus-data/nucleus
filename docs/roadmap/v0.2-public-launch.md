# v0.2 — Public Launch

> **Codename**: Public Launch
> **Target**: 2026 Q3 (Month 8-14 of founder's timeline)
> **LOC ceiling**: 12,000 (start of phase: ~4,200 shipped in v0.1)
> **Persona**: Startup data team 5-20 engineers (same beachhead as v0.1)
> **Theme**: Ship the editorial hero UI, complete the docs site, enable scheduled assets, and add the connectors that beachhead teams need next.

---

## Theme: From Beta to Public

v0.1 proved the beachhead: WSL E2E 8/8 PASS, 7 s boot, real Iceberg snapshot. v0.2 is the public launch — the release that goes on the website, gets the GitHub "Star us" campaign, and earns the first 100 external users.

This phase ships four things that matter for the public launch:

1. **Workbench MVP** — the editorial hero UI (Monaco editor, asset graph, chat panel) per [ADR-016](../decisions/ADR-016-workbench-mvp.md).
2. **AI Copilot chat in the UI** — not just CLI; Workbench-native chat per [ADR-015](../decisions/ADR-015-ai-chat-mvp.md).
3. **Docs site** — the `docs/site/` content rendered at `nucleus.dev` (MkDocs Material or equivalent).
4. **Scheduled assets** — `@nucleus.asset(schedule="@daily")` daemon-mode (`nucleus schedule on/off/trigger`) per [ADR-017](../decisions/ADR-017-schedule-exposure-v01.md).

---

## Features

### Workbench MVP (ADR-016, Fork B)

Per `nucleus_architecture_v4.1.md` §8.1 and [ADR-016](../decisions/ADR-016-workbench-mvp.md):

- React + Vite + TypeScript frontend (Vite for fast HMR)
- Monaco editor (same as VS Code) — asset editing with `@nucleus.asset` syntax awareness
- Asset graph visualizer (xyflow / React Flow) — dependency graph rendered from the project's asset registry
- Chat panel — wraps `nucleus chat` via the same LiteLLM backend as CLI
- FastAPI backend (already scaffolded in v0.1) — serves REST API to the frontend
- Run trigger UI — click-to-materialize individual assets
- Schema browser — inspect asset schema contracts

**What is NOT in Workbench v0.2** (deferred to v0.3+):
- Full schema-aware Copilot completion (needs lineage data — v0.3+)
- Marimo notebook embed (v0.3+)
- Multi-user collaboration (v1.5+)

### Scheduled Assets Daemon (ADR-017)

- `nucleus schedule on <asset-key>` — registers the schedule registered at decoration time
- `nucleus schedule off <asset-key>` — de-registers
- `nucleus schedule trigger <asset-key>` — one-shot manual fire
- `nucleus schedule list` — show all registered schedules + next-run times
- `nucleus schedule preview <asset-key> [--n 5]` — preview next N runs (works in v0.1.1 already)
- Background daemon (`nucleus schedule daemon`) — `croniter`-driven, Dagster-backed scheduler loop

### Connector Batch 2

Per v4.1 §18.2 and the beachhead team's next data sources:

- **BigQuery** (`nucleus ingest bigquery://...`) — via dlt `google_analytics` / BigQuery source; requires `google-cloud-bigquery` extras
- **SFTP / local files** — Parquet and CSV direct ingest (already partially in v0.1 SQLite path; extend to Parquet/CSV without SQLAlchemy)
- **Snowflake read** (`nucleus ingest snowflake://...`) — read-only; Snowflake as a *source* (yield-to-giants Mode 1 complement)

### Docs Site

- Deploy `docs/site/` to `nucleus.dev` (GitHub Pages or Cloudflare Pages)
- Core content: getting-started guide, quickstart, `ctx` SDK reference, CLI reference, connector guides
- API reference auto-generated from docstrings (mkdocstrings or sphinx)

### CI / CD Hardening

- Publish to PyPI via GitHub Actions OIDC (no stored API keys)
- GitHub Release auto-created from `CHANGELOG.md` section
- GitHub Pages deploy for docs on every tag

---

## Pre-requisites

This phase starts AFTER:

- [ ] PoC #5 external-tester field test completed (at least 3 testers, 2/3 PASS ≤ 30 min) — founder-gated
- [ ] v0.1.0 tag on PyPI (beta) — founder-gated
- [ ] ADR-017 (schedule exposure) ratified
- [ ] ADR-016 (Workbench MVP) open questions resolved (Workbench tech stack: Vite/React confirmed)
- [ ] `docs/site/` content skeleton authored (getting-started, quickstart, CLI reference)

---

## Go/No-Go Release Gates

All **must** be green before tagging `v0.2.0`:

```
[ ] All governance scripts EXIT 0:
      scripts/check_vocabulary.py
      scripts/check_pinning.py
      scripts/loc_budget.py         (src/nucleus/ < 12,000 LOC)
      scripts/dagster_leak_check.py
      scripts/check_error_codes.py
      scripts/check_api_stability.py
      scripts/check_licenses.py

[ ] pytest tests/ -q → 0 failures (may have skips for v0.3+ features)

[ ] scripts/beachhead_e2e.py → 8/8 PASS (boot < 10 s still)

[ ] Workbench E2E: nucleus up → localhost:8080 returns HTTP 200

[ ] docs site deploys: nucleus.dev/getting-started/ returns 200

[ ] scheduled asset fires correctly in daemon mode for ≥ 1 test schedule

[ ] PyPI publish dry-run succeeds (--dry-run mode)

[ ] Founder sign-off
```

---

## Known Risks

| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| Workbench frontend scope expands (designer wants more than MVP) | Medium | High | Hard-cut at Monaco + asset graph + chat panel. Anything else is v0.3+. |
| Scheduled assets daemon conflicts with embedded Dagster | Medium | Medium | Daemon uses the same Dagster wrapped substrate; schedule resolution is croniter (already tested). Fallback: mini-scheduler approach if Dagster loop creates issues. |
| Docs site content lag | High | Medium | 5 core pages minimum for launch; defer advanced guides to v0.3. |
| BigQuery connector auth complexity | Medium | Low | BigQuery extras are opt-in (`pip install nucleus[bigquery]`); if auth is a rabbit hole, defer to v0.3 and ship SFTP/CSV only. |
| PyPI publish requires trust setup | Low | Medium | Use GitHub OIDC (no stored secrets). Test `--dry-run` before actual publish. |

---

## Cutover Plan + Rollback

1. Tag `v0.2.0-rc1` internally; run full test suite + beachhead E2E.
2. Deploy docs site behind staging URL; validate all links.
3. Publish to TestPyPI: `pip install -i https://test.pypi.org/simple/ nucleus==0.2.0rc1`.
4. Run PoC #5 with RC version (if testers haven't done v0.1 yet).
5. On founder go-signal: tag `v0.2.0`, CI auto-publishes to PyPI.
6. Monitor GitHub Issues for 48 h.

**Rollback**: `pip install nucleus==0.1.0` (documented on the release page). PyPI yank if critical regression.

---

## Success Metrics

- PoC #5 external testers: 3+ complete runs, 2/3 ≤ 30 min with v0.2 Workbench
- GitHub stars: 100+ within 2 weeks of launch announcement (# NEEDS VERIFICATION — actual target TBD by founder)
- `pip install nucleus` clean install in <60 s on a 100 Mbps connection
- Workbench UI loads in <3 s on localhost

---

## What Is Intentionally NOT in v0.2

These are explicitly deferred to v0.3+:

- **dlt 100+ connectors** — dlt integration is v0.3; v0.2 uses `ctx.copy_from` only
- **Lakekeeper REST catalog** — still filesystem catalog; Lakekeeper is v0.3
- **Marimo notebook integration** — v0.3
- **Schema-aware Copilot completion** — needs lineage data from v0.3
- **Column-level lineage** — v0.5+
- **Multi-user auth** — v1.5+
- **`ctx.write`, `ctx.log`, `ctx.params`** — deferred from v0.1; land when ADR-013 ctx runtime object lands in v0.2 or v0.3

---

## Step-by-Step for a Developer Joining v0.2

1. Read [`AGENTS.md`](../../AGENTS.md) — mandatory, 30 min.
2. Read this roadmap doc (you're doing it).
3. Read [`nucleus_architecture_v4.1.md`](../../nucleus_architecture_v4.1.md) §8 (Experience layer) and §18.2 (v0.2 roadmap).
4. Read [`CHANGELOG.md`](../../CHANGELOG.md) — know what shipped in v0.1.0.
5. Run the beachhead E2E: `python scripts/beachhead_e2e.py` — verify your environment.
6. Pick a task from [`docs/FOUNDER_ACTION_QUEUE.md`](../FOUNDER_ACTION_QUEUE.md) or a GitHub Issue labeled `v0.2`.
7. Follow the relevant dev guide:
   - Adding a connector? → [`../dev-guides/03-add-connector.md`](../dev-guides/03-add-connector.md)
   - Adding a CLI command? → [`../dev-guides/04-add-cli-command.md`](../dev-guides/04-add-cli-command.md)
   - Authoring an ADR? → [`../dev-guides/08-author-adr.md`](../dev-guides/08-author-adr.md)
   - Working with AI (Cursor)? → [`../dev-guides/12-ai-pair-programming.md`](../dev-guides/12-ai-pair-programming.md)

---

*Source: `nucleus_architecture_v4.1.md` §18.2. ADR-016 (Workbench MVP), ADR-015 (AI chat), ADR-017 (schedules).*
